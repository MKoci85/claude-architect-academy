{
  "sections": [
    {
      "title": "Claude on AWS Bedrock",
      "articles": [
        {
          "title": "Accessing Claude Through AWS Bedrock",
          "subsections": [
            {
              "title": "Anthropic API vs. AWS Bedrock",
              "blocks": [
                {
                  "type": "callout",
                  "style": "info",
                  "title": "They aren't the same",
                  "content": "Calling Claude directly against the Anthropic API (api.anthropic.com) and calling Claude through AWS Bedrock are two different integrations. Bedrock is the AWS service that exposes several foundation models (including Claude) under an AWS-native interface and billing, with authentication via AWS credentials (IAM) instead of an Anthropic API key."
                }
              ]
            },
            {
              "title": "The flow of a chat app",
              "blocks": [
                {
                  "type": "text",
                  "content": "In a typical chat app, the user sees a simple interface — they type a message and get a response — but behind it there's a multi-step flow between the browser, your server, and AWS Bedrock."
                },
                {
                  "type": "steps",
                  "steps": [
                    {
                      "step": 1,
                      "title": "The user sends their message",
                      "description": "Through the app's web interface"
                    },
                    {
                      "step": 2,
                      "title": "The server receives the request",
                      "description": "With the user's message text"
                    },
                    {
                      "step": 3,
                      "title": "The server calls AWS Bedrock",
                      "description": "Using the Bedrock client to make the request"
                    },
                    {
                      "step": 4,
                      "title": "The request includes the message and the model ID",
                      "description": "For example, the ID corresponding to Claude Haiku or Claude Sonnet on Bedrock"
                    },
                    {
                      "step": 5,
                      "title": "The model processes the request",
                      "description": "And generates the response text"
                    },
                    {
                      "step": 6,
                      "title": "Bedrock returns an assistant message",
                      "description": "With the response generated by the model"
                    },
                    {
                      "step": 7,
                      "title": "The server forwards the response to the browser",
                      "description": "And the user sees it in the chat interface"
                    }
                  ]
                },
                {
                  "type": "callout",
                  "style": "tip",
                  "title": "Your server is the intermediary",
                  "content": "The user's browser never talks directly to AWS Bedrock. Your backend server is the one holding the AWS credentials and making the request to the model — the same intermediation pattern used with the direct Anthropic API or with Vertex AI, to avoid exposing credentials on the client."
                }
              ]
            }
          ]
        },
        {
          "title": "Your First Request to AWS Bedrock",
          "subsections": [
            {
              "title": "The three components of a request",
              "blocks": [
                {
                  "type": "text",
                  "content": "Making a first request to Bedrock requires three components: a Bedrock Runtime client to connect to the service, a Model ID specifying which model you want to run, and a User Message with the text you're sending to the model."
                }
              ]
            },
            {
              "title": "Configuring the Bedrock client",
              "blocks": [
                {
                  "type": "text",
                  "content": "AWS's Python SDK (boto3) is what's used to connect to Bedrock — not the Anthropic SDK. A client is created for the `bedrock-runtime` service, specifying the AWS region:"
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "import boto3\n\nclient = boto3.client(\"bedrock-runtime\", region_name=\"us-west-2\")"
                }
              ]
            },
            {
              "title": "Model IDs and regional availability",
              "blocks": [
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "Not every model is in every region",
                  "content": "If you try to invoke a model that doesn't exist in the region configured on the client, the request fails with a cryptic 'model does not exist' type error. For example, if Claude Sonnet is available in us-west-2 but the client points to us-east-1, the request will fail."
                }
              ]
            },
            {
              "title": "Inference profiles: the solution to the regional problem",
              "blocks": [
                {
                  "type": "text",
                  "content": "An inference profile solves the regional availability problem by automatically routing the request to a region where the chosen model is actually available. Instead of manually keeping track of which model is in which region, the inference profile already knows the model is hosted in several regions (for example us-west-2 and us-east-2) and AWS picks the right one for you, even if the client connected from a different region."
                },
                {
                  "type": "text",
                  "content": "AWS calls this **cross-Region inference (CRIS)**. An inference profile defines a model and the set of (destination) regions Bedrock can route the request to. The ID follows a format with a geographic prefix + the model ID, for example:"
                },
                {
                  "type": "code",
                  "language": "text",
                  "code": "us.anthropic.claude-3-haiku-20240307-v1:0\neu.anthropic.claude-sonnet-4-20250514-v1:0"
                },
                {
                  "type": "callout",
                  "style": "tip",
                  "title": "Where to find the inference profile ID",
                  "content": "In the AWS Bedrock console, the inference profile ID isn't in the main model catalog — you have to look for it under the 'Cross-region inference' section, or on each model's detail page, 'Regional availability' table → 'Inference profile IDs'. That's the ID used as `modelId` in the request, not the general catalog ID."
                },
                {
                  "type": "compare",
                  "title": "Geographic vs. Global cross-Region inference",
                  "items": [
                    {
                      "title": "Geographic (us., eu., apac.)",
                      "rows": [
                        "Routes only within the prefix's geography (US, EU, APAC, etc.)",
                        "Standard model pricing",
                        "Designed for data residency / regional compliance requirements"
                      ]
                    },
                    {
                      "title": "Global (global.)",
                      "rows": [
                        "Routes to any commercial AWS region worldwide",
                        "Roughly 10% cheaper than standard pricing",
                        "Designed to optimize cost when there's no data residency restriction",
                        "Currently only available for Claude Sonnet 4"
                      ]
                    }
                  ]
                },
                {
                  "type": "callout",
                  "style": "info",
                  "title": "Details worth knowing",
                  "content": "There's no extra cost for the routing itself — pricing is calculated based on the region you invoke the inference profile from, not the actual destination region. Cross-Region inference can route to regions that aren't manually enabled in your AWS account (no need to opt in to each one). All cross-region traffic travels over AWS's internal network, encrypted, without going over the public internet. CloudTrail logs these requests in the origin region, with the `additionalEventData.inferenceRegion` field indicating where it was actually processed."
                }
              ]
            },
            {
              "title": "Structure of a user message",
              "blocks": [
                {
                  "type": "text",
                  "content": "User messages in Bedrock have a structure that might seem overengineered at first, but it has a reason:"
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "user_message = {\n    \"role\": \"user\",\n    \"content\": [\n        {\"text\": \"What's 1+1?\"}\n    ]\n}"
                },
                {
                  "type": "text",
                  "content": "`content` is a list because a single message can combine different types of content — text, images, or other media types. That structure is what allows building multimodal requests."
                }
              ]
            },
            {
              "title": "Making the request with converse",
              "blocks": [
                {
                  "type": "code",
                  "language": "python",
                  "code": "response = client.converse(\n    modelId=model_id,\n    messages=[user_message]\n)"
                },
                {
                  "type": "text",
                  "content": "The method used to invoke the model is `converse`, not `messages.create` like in the Anthropic SDK. The response carries a fair amount of metadata; to get to the generated text you have to navigate the response structure:"
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "response[\"output\"][\"message\"][\"content\"][0][\"text\"]"
                }
              ]
            },
            {
              "title": "User messages vs. assistant messages",
              "blocks": [
                {
                  "type": "text",
                  "content": "There are two main message types: **user messages** (content you send to the model, `role: \"user\"`) and **assistant messages** (content the model generated, `role: \"assistant\"`). Both share the same `role` + `content` list structure."
                },
                {
                  "type": "callout",
                  "style": "info",
                  "title": "Same shape, different role",
                  "content": "The assistant message Bedrock returns follows exactly the same format as the user message you sent, only the `role` changes. That symmetry is what makes it easy to chain messages to build multi-turn conversations, reusing each model response as part of the next request's history."
                }
              ]
            }
          ]
        },
        {
          "title": "Multi-Turn Conversations",
          "subsections": [
            {
              "title": "Bedrock doesn't save messages",
              "blocks": [
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "Every request is independent",
                  "content": "Neither Bedrock nor Claude stores the messages you send or the responses you receive. Every call to the Converse API is completely stateless — if you send only the follow-up question 'And 3 more?' without the prior history, the model has no way to know what '3 more' refers to."
                },
                {
                  "type": "text",
                  "content": "To maintain a conversation's context, your application has to keep the full list of messages and resend it entirely on every request — including both the user messages and the model responses you already received."
                }
              ]
            },
            {
              "title": "Helper functions to manage messages",
              "blocks": [
                {
                  "type": "code",
                  "language": "python",
                  "code": "def add_user_message(messages, text):\n    messages.append({\n        \"role\": \"user\",\n        \"content\": [{\"text\": text}]\n    })\n\ndef add_assistant_message(messages, text):\n    messages.append({\n        \"role\": \"assistant\",\n        \"content\": [{\"text\": text}]\n    })\n\ndef chat(messages):\n    response = client.converse(\n        modelId=model_id,\n        messages=messages\n    )\n    return response[\"output\"][\"message\"][\"content\"][0][\"text\"]"
                }
              ]
            },
            {
              "title": "Building the conversation step by step",
              "blocks": [
                {
                  "type": "code",
                  "language": "python",
                  "code": "messages = []\n\nadd_user_message(messages, \"What's 1+1?\")\nanswer = chat(messages)\nadd_assistant_message(messages, answer)\n\nadd_user_message(messages, \"And 3 more added to that?\")\nanswer = chat(messages)\nprint(answer)\n# \"Starting with the result of 1+1 = 2, if we add 3 more to that, we get: 2 + 3 = 5\""
                },
                {
                  "type": "text",
                  "content": "Each model response is appended back to `messages` as an assistant message before the next question. It's that complete history — not just the last message — that gives Claude the context to understand that '3 more' is added to the previous result."
                }
              ]
            },
            {
              "title": "Roles have to strictly alternate",
              "blocks": [
                {
                  "type": "text",
                  "content": "The message list must follow the pattern user → assistant → user → assistant. Never two user messages in a row, nor two assistant messages in a row."
                },
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "It's not just a convention, it's an API validation",
                  "content": "If you break the alternation — for example, adding two user messages in a row without an assistant response in between — the Converse API rejects the request with an explicit `ValidationException` ('roles must alternate between \"user\" and \"assistant\"'). If you need to send additional content with the same role, you have to combine it into a single message before sending it, not add it as a separate message."
                }
              ]
            }
          ]
        },
        {
          "title": "System Prompts in Bedrock",
          "subsections": [
            {
              "title": "The Converse API's system parameter",
              "blocks": [
                {
                  "type": "callout",
                  "style": "info",
                  "title": "Same concept as in the Anthropic API",
                  "content": "Assigning Claude a role via a system prompt instead of listing rules in every user message is the same **role prompting** technique already explained in Claude API → Prompt Engineering → Role prompting: the role conditions vocabulary, level of detail, and criteria consistently, without having to anticipate every possible case in the user's message. What changes in Bedrock is just how it's passed to the request."
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "system_prompt = \"\"\"\nYou are an AWS cloud support specialist. Your job is to answer user queries related \nto cloud hosting services on AWS.\n\"\"\"\n\nresponse = client.converse(\n    modelId=model_id,\n    messages=messages,\n    system=[{\"text\": system_prompt}]\n)"
                },
                {
                  "type": "text",
                  "content": "Unlike the Anthropic SDK (where `system` is a string), in Bedrock's Converse API `system` is a **list** of `SystemContentBlock` — objects with a `text` key. In practice a single text block is almost always used, but the list structure is the same reason `content` is a list in messages: it leaves room for other block types, like `cachePoint` (prompt caching) or `guardContent` (content to be evaluated by a guardrail)."
                },
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "The system prompt text can't be an empty string",
                  "content": "The `text` field of `SystemContentBlock` has a minimum of 1 character. Passing `system=[{\"text\": \"\"}]` is invalid — if there's no system prompt, you should omit the `system` parameter entirely instead of sending it empty."
                }
              ]
            },
            {
              "title": "chat() function with optional system prompt",
              "blocks": [
                {
                  "type": "code",
                  "language": "python",
                  "code": "def chat(messages, system=None):\n    params = {\"modelId\": model_id, \"messages\": messages}\n\n    if system:\n        params[\"system\"] = [{\"text\": system}]\n\n    response = client.converse(**params)\n\n    return response[\"output\"][\"message\"][\"content\"][0][\"text\"]"
                },
                {
                  "type": "text",
                  "content": "The `if system` check is exactly what avoids the empty-string problem: if no system prompt is passed, the `system` key isn't even included in `params`, instead of sending it as an empty list or empty text."
                }
              ]
            },
            {
              "title": "Specification vs. role: a concrete example",
              "blocks": [
                {
                  "type": "text",
                  "content": "For an AWS support assistant, the alternative to a system prompt would look something like this in every user message — fragile because you have to anticipate every case, and the user sees all the internal instructions mixed in with their question:"
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "add_user_message(messages, f\"\"\"\nDO:\n  - Mention how to host things on AWS\n  - List some setup steps\n  - Mention related AWS services\n\nDONT:\n  - Mention how to host services outside AWS\n  - Answer questions not related to AWS\n\n{users_question}\n\"\"\")"
                },
                {
                  "type": "text",
                  "content": "The effect shows even on questions that are on-topic but generic. Without a system prompt, \"How do I host a Postgres database?\" gets a broad answer comparing several cloud providers and self-hosted options. With the AWS-specialist system prompt active, the same question produces an answer scoped only to AWS solutions — RDS, Aurora, EC2 deployments — without mentioning competitors, and with AWS-specific setup steps."
                },
                {
                  "type": "text",
                  "content": "With the same system prompt, an off-topic question like \"give me a bread recipe\" doesn't get answered — Claude declines and stays in its role, with no need for an explicit rule for that particular case. That's the advantage over the rule-list approach: it covers both scoping related topics and rejecting unrelated ones, without having to anticipate every case."
                },
                {
                  "type": "callout",
                  "style": "tip",
                  "title": "The system prompt is fixed per conversation, not per turn",
                  "content": "You can change the system prompt between one conversation and another, but not mid-conversation — it's applied once, before the model sees any user message, and stays fixed for that entire message thread."
                }
              ]
            }
          ]
        },
        {
          "title": "Inference Parameters: temperature and Friends",
          "subsections": [
            {
              "title": "inferenceConfig instead of a flat parameter",
              "blocks": [
                {
                  "type": "callout",
                  "style": "info",
                  "title": "Same concept, different location in the request",
                  "content": "temperature controls how deterministic or varied the model's output is — the same parameter already documented in Claude API → API Reference. What changes in Bedrock's Converse API is where it goes: it isn't a flat request field like in the Anthropic SDK, but nested inside `inferenceConfig`."
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "def chat(messages, system=None, temperature=1.0):\n    params = {\n        \"modelId\": model_id,\n        \"messages\": messages,\n        \"inferenceConfig\": {\"temperature\": temperature}\n    }\n\n    if system:\n        params[\"system\"] = [{\"text\": system}]\n\n    response = client.converse(**params)\n    return response[\"output\"][\"message\"][\"content\"][0][\"text\"]"
                },
                {
                  "type": "text",
                  "content": "`inferenceConfig` is the same object where the rest of the Converse API's sampling parameters go — not just `temperature`, but also `maxTokens`, `topP`, and `stopSequences`."
                }
              ]
            },
            {
              "title": "The default depends on the model, not on Bedrock",
              "blocks": [
                {
                  "type": "text",
                  "content": "If you omit `temperature`, the Converse API doesn't apply a fixed Bedrock-specific value — it uses the default of the model you're invoking. For current Claude models, that default is 1.0, same as in the direct Anthropic API."
                },
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "On Bedrock, the mutual exclusion with top_p applies as of Sonnet 4.5 and Haiku 4.5 — not as of Opus 4.7",
                  "content": "The direct Anthropic API blocks sending a non-default value of temperature, top_p, or top_k with a 400 error as of Opus 4.7 (see Claude API → API Reference). On Bedrock the restriction is different: Claude Sonnet 4.5 and Claude Haiku 4.5 only allow specifying temperature or top_p, not both at once, and this doesn't apply to earlier models. It's the same principle — adjusting a single sampling parameter at a time — but with a different trigger depending on the platform."
                }
              ]
            },
            {
              "title": "top_k doesn't live in inferenceConfig",
              "blocks": [
                {
                  "type": "text",
                  "content": "Unlike `temperature`, `maxTokens`, `topP`, and `stopSequences`, `top_k` isn't a base parameter of the Converse API — `inferenceConfig` has no `topK` field. For models that do support it, like Claude, you have to pass it separately in `additionalModelRequestFields`."
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "response = client.converse(\n    modelId=model_id,\n    messages=messages,\n    inferenceConfig={\"temperature\": 0.5},\n    additionalModelRequestFields={\"top_k\": 200}\n)"
                }
              ]
            },
            {
              "title": "stopSequences: cutting generation at a marker",
              "blocks": [
                {
                  "type": "code",
                  "language": "python",
                  "code": "def chat(messages, system=None, temperature=1.0, stop_sequences=None):\n    params = {\n        \"modelId\": model_id,\n        \"messages\": messages,\n        \"inferenceConfig\": {\"temperature\": temperature}\n    }\n\n    if stop_sequences:\n        params[\"inferenceConfig\"][\"stopSequences\"] = stop_sequences\n\n    if system:\n        params[\"system\"] = [{\"text\": system}]\n\n    response = client.converse(**params)\n    return response[\"output\"][\"message\"][\"content\"][0][\"text\"]"
                },
                {
                  "type": "text",
                  "content": "As soon as Claude generates any of the strings passed in `stopSequences`, it immediately cuts off generation and returns only what was generated up to that point — the sequence that triggered the cut isn't included in the text. For example, asking it to count from 1 to 10 with `stop_sequences=[\"5\"]` returns \"1, 2, 3, 4, \" without the \"5\". With several stop sequences at once, Claude cuts at whichever appears first."
                },
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "To know which stop sequence triggered the cut, you have to request it explicitly",
                  "content": "`response[\"stopReason\"]` is `\"stop_sequence\"` when this is why it cut off, but the actual value of which sequence matched doesn't travel in the response by default. You have to declare it in `additionalModelResponseFieldPaths: [\"/stop_sequence\"]` when building the request, and it then shows up in `response[\"additionalModelResponseFields\"][\"stop_sequence\"]`. Unlike the direct Anthropic API, where `stop_sequence` is a top-level field on every response, in Bedrock it's opt-in."
                }
              ]
            }
          ]
        },
        {
          "title": "Streaming with converse_stream",
          "subsections": [
            {
              "title": "converse_stream returns a generator",
              "blocks": [
                {
                  "type": "callout",
                  "style": "info",
                  "title": "The concept of streaming is already covered in Claude API → Practical Guides",
                  "content": "Why use streaming (incremental response UX instead of waiting 10-30 seconds) is the same reason on any platform. What changes in Bedrock is the concrete shape of the method and the events."
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "messages = []\nadd_user_message(messages, \"Write a 1 sentence description of a fake database\")\nresponse = client.converse_stream(messages=messages, modelId=model_id)\n\nfor event in response[\"stream\"]:\n    print(event)"
                },
                {
                  "type": "text",
                  "content": "`converse_stream` (not `converse`) immediately returns a dict with a `stream` key, which is an event generator — each one a dict with a single key identifying the event type. To extract just the text, you filter by `contentBlockDelta` and navigate down to `delta.text`:"
                },
                {
                  "type": "code",
                  "language": "python",
                  "code": "text = \"\"\nfor event in response[\"stream\"]:\n    if \"contentBlockDelta\" in event:\n        chunk = event[\"contentBlockDelta\"][\"delta\"][\"text\"]\n        print(chunk, end=\"\")\n        text += chunk"
                }
              ]
            },
            {
              "title": "The full event set goes beyond contentBlockDelta",
              "blocks": [
                {
                  "type": "table",
                  "head": ["Event", "Description"],
                  "rows": [
                    ["messageStart", "Starts a new message."],
                    ["contentBlockStart", "Starts a new content block — text, tool_use, or another type."],
                    ["contentBlockDelta", "Incremental fragment of the current content block. The one most used to show live text."],
                    ["contentBlockStop", "The current content block finished."],
                    ["messageStop", "The message finished. Carries the final `stopReason`."],
                    ["metadata", "Closes the stream with `usage` (tokens) and latency metrics."]
                  ]
                },
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "There's no event equivalent to message_delta",
                  "content": "In the direct Anthropic API (see Claude API → API Reference), `message_delta` carries the `stop_reason` and the `usage` count together, before `message_stop`. In Bedrock that information is split differently: `stopReason` travels inside `messageStop`, and `usage` along with latency only arrive in the `metadata` event at the end of the stream — there's no single event that combines both."
                }
              ]
            },
            {
              "title": "Errors also arrive as stream events",
              "blocks": [
                {
                  "type": "callout",
                  "style": "warning",
                  "title": "ConverseStream can end in an exception within the stream, not just a prior HTTP error",
                  "content": "If something fails during generation — throttling, an internal model error, a validation issue — the event can arrive as an exception key within the same stream (`throttlingException`, `validationException`, `modelStreamErrorException`, `internalServerException`, `serviceUnavailableException`), not as an error HTTP status before events start arriving. The code consuming the stream has to check for these keys in addition to `contentBlockDelta`, or the error goes unnoticed."
                }
              ]
            },
            {
              "title": "Streaming doesn't change the cost",
              "blocks": [
                {
                  "type": "callout",
                  "style": "tip",
                  "title": "Billed by tokens generated, not by how they're delivered",
                  "content": "Using `converse_stream` instead of `converse` doesn't change the price: the model generates the same tokens, streaming only changes the transport (they arrive in chunks via events instead of all together at the end). The `usage` count that arrives in the `metadata` event reflects the same calculation you'd get without streaming. If your application closes the connection as soon as it has what it needs, it can even come out cheaper — you're only billed for the tokens generated up to that point, not for a full response that was never finished generating."
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}
